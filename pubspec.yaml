name: clean_architecture
description: overall project architecture project with riverpod.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  go_router: ^10.1.2
  connectivity_plus: ^4.0.1
  cupertino_icons: ^1.0.5
  # easy_localization: ^3.0.3
  hive_flutter: ^1.1.0
  flutter_screenutil: ^5.9.0
  flutter_localizations:
    sdk: flutter
  dartz: ^0.10.1
  dio: ^5.3.2
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  cached_network_image: ^3.2.3
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  clock: ^1.1.1
  riverpod_annotation: ^2.1.1
  shared_preferences: ^2.2.0
  state_notifier: ^0.7.2+1

dev_dependencies:
  build_runner: ^2.4.6
  custom_lint:
  riverpod_generator: ^2.2.4
  riverpod_lint: ^1.4.0
  flutter_lints: ^2.0.2
  flutter_test:
    sdk: flutter
  freezed: ^2.2.0
  # http_mock_adapter is a simple to use mocking package for Dio intended to be used in tests. It provides various types and methods to declaratively mock request-response communication.
  http_mock_adapter: ^0.4.4
  json_serializable: ^6.5.3
  # Mock library for Dart,focus on providing a familiar api, to create simulations in dart
  mocktail: ^0.3.0
  hive_test: ^1.0.1
  go_router_builder: ^2.0.1
  state_notifier_test: ^0.0.9
  #very_good_analysis,this package provides lint rules for Dart and Flutter which are used at Very Good Ventures
  very_good_analysis: ^5.0.0+1
  test_coverage_badge: ^0.2.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/lang/

